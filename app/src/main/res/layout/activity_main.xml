<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity" >

    <androidx.fragment.app.FragmentContainerView

        android:id="@+id/navHostFragmentMain"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        app:defaultNavHost="true"
        app:navGraph="@navigation/nav_graph_main"/>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationViewMain"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:background="@color/primary20"
        app:itemActiveIndicatorStyle="@null"
        app:itemIconSize="28dp"
        app:itemIconTint="@color/bottom_navigation_view_item_tint"
        app:itemTextColor="@color/bottom_navigation_view_item_tint"
        app:itemBackground="@color/primary20"
        app:labelVisibilityMode="labeled"
        app:itemTextAppearanceActive="@style/bottom_navigation_view_item_text_appearance"
        app:itemTextAppearanceInactive="@style/bottom_navigation_view_item_text_appearance"
        app:menu="@menu/bottom_menu_main"
        android:visibility="visible"/>

</LinearLayout>